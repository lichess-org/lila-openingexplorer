# Copy this file to application.conf
# This is environment specific config, it overrides base.conf

# Format documentation:
# https://github.com/typesafehub/config#using-hocon-the-json-superset

include "base"

# Configure a secret key for production usage
# play.http.secret.key = "secret"

# Send header Access-Control-Allow-Origin "*"
# explorer.corsHeader = true

# Disable stat cache
# explorer.statCache.ttls = 1 ms

# Disable cache
# explorer.cache.maxMoves = 0
